logging:
  level: INFO
  path: ./outputs/logs
paths:
  features: ./outputs/features_baseline
  tokenized: ./outputs/tokenized_baseline
  initial_pids: pids_held_out.pt
  outcomes: ./outputs/outcomes/
  outcome: MORTALITY.csv
  cohort: ./outputs/cohort/held_out
selection:
  exclude_prior_outcomes: true
  exposed_only: false
  age: {min_years: 18, max_years: 120}
index_date:
  mode: relative
  relative: {n_hours_from_exposure: -24}
test_ratio: 1.0
