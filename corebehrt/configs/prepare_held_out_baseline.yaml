logging:
  level: INFO
  path: ./outputs/logs
paths:
  features: ./outputs/features_baseline
  tokenized: ./outputs/tokenized_baseline
  cohort: ./outputs/cohort/held_out/
  outcomes: ./outputs/outcomes
  outcome: MORTALITY.csv
  prepared_data: ./outputs/testing/held_out/processed_data_baseline/
data:
  type: "test"
  test_modes: ["held_out"]
  truncation_len: 30
  min_len: 2
outcome:
  n_hours_censoring: -10
  n_hours_start_follow_up: 1
  n_hours_end_follow_up: null
concept_pattern_hours_delay:
  "^D(?!OD)": 72   # diagnosis recording-lag grace, but NOT the DOD outcome code
